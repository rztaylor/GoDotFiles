---
description: Build and install gdf locally
---

1. Build with version metadata from `Makefile`:
`make build`

2. Install to `~/bin` via project target:
`make install`

3. Verify build output and version info:
- `./gdf --version`
- `~/bin/gdf --help`
