---
description: Build and install gdf locally
---
// turbo-all
1. Build the binary:
   `go build -o gdf ./cmd/gdf`

2. Install to GOPATH/bin:
   `go install ./cmd/gdf`

3. Verify installation:
   `gdf --help`
