kind: Recipe/v1
name: delta
description: A viewer for git and diff output
package:
  brew: git-delta
  apt:
    name: git-delta
  dnf: git-delta
dotfiles:
  - source: delta/config
    target: .config/delta/config
