kind: Recipe/v1
name: aws-cli
description: Universal Command Line Interface for Amazon Web Services
package:
  brew: awscli
  apt:
    name: awscli
  dnf: aws-cli
dotfiles:
  - source: aws/config
    target: .aws/config

